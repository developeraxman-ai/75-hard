export function FailureHistory({logs,events}){const failed=logs.filter(l=>l.failed); return <section className="card"><h2 className="text-xl font-black">Failure history</h2><p className="mt-1 text-stone-400">Failure is data. Spiral is optional.</p><ul className="mt-4 space-y-2 text-sm">{failed.map(l=><li key={l._id} className="text-red-200">Day {l.dayNumber}: {l.failureReason||'Manual failure'}</li>)}{events.filter(e=>e.finalChoice==='impulse').map(e=><li key={e._id} className="text-red-200">Impulse chose: {e.type}</li>)}{!failed.length&&!events.some(e=>e.finalChoice==='impulse')&&<li className="text-stone-500">No failures recorded.</li>}</ul></section>}
