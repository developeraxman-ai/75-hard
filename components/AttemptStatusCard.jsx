export function AttemptStatusCard({attempt,log}){return <section className="card bg-gradient-to-br from-command-card to-black"><div className="flex items-end justify-between"><div><p className="text-sm uppercase tracking-widest text-stone-500">Active attempt</p><h2 className="text-4xl font-black">Day {attempt.currentDay||log?.dayNumber} / 75</h2></div><span className="rounded-full border border-command-line px-3 py-1 text-xs uppercase text-command-gold">{attempt.status}</span></div>{log?.completed&&<p className="mt-4 text-command-gold">Day {log.dayNumber} evidence collected.</p>}{log?.failed&&<p className="mt-4 text-red-300">Day failed. Restart tomorrow morning. No spiral.</p>}</section>}
