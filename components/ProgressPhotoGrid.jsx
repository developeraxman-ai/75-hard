import {thumbnailUrl} from '@/lib/cloudinary';
export function ProgressPhotoGrid({logs}){const photos=logs.filter(l=>l.progressPhotoUrl); return <div className="grid grid-cols-2 gap-3">{photos.length?photos.map(l=><figure key={l._id} className="overflow-hidden rounded-2xl border border-command-line bg-black"><img src={thumbnailUrl(l.progressPhotoUrl)} alt={`Progress day ${l.dayNumber}`} className="aspect-[4/5] w-full object-cover brightness-75"/><figcaption className="p-2 text-xs text-stone-300">Day {l.dayNumber}</figcaption></figure>):<p className="text-stone-500">No evidence photos yet.</p>}</div>}
