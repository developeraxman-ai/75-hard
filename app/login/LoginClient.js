'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export function LoginClient({hasUser}){const router=useRouter(); const[register,setRegister]=useState(!hasUser); const[form,setForm]=useState({name:'',email:'',password:'',startDate:new Date().toISOString().slice(0,10)}); const[err,setErr]=useState(''); async function submit(e){e.preventDefault(); setErr(''); const url=register?'/api/auth/register':'/api/auth/login'; const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)}); const j=await r.json().catch(()=>({})); if(r.ok) router.push('/dashboard'); else setErr(j.error||'Authentication failed')} return <form onSubmit={submit} className="mt-6 space-y-3">{register&&<input className="input" placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/>}<input className="input" type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{register&&<label className="block text-sm text-stone-400">Start date<input className="input mt-1" type="date" value={form.startDate} onChange={e=>setForm({...form,startDate:e.target.value})}/></label>}<button className="btn-primary w-full">{register?'Register first user':'Login'}</button>{hasUser&&<button type="button" className="btn-ghost w-full" onClick={()=>setRegister(!register)}>{register?'Use login':'Register first user'}</button>}{err&&<p className="text-red-300">{err}</p>}</form>}
