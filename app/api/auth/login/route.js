import {NextResponse} from 'next/server'; import bcrypt from 'bcryptjs'; import {connectDb} from '@/lib/db'; import User from '@/models/User'; import {loginSchema} from '@/schemas'; import {setSession} from '@/lib/auth';
export async function POST(req){ await connectDb(); const data=loginSchema.parse(await req.json()); const user=await User.findOne({email:data.email.toLowerCase()}); if(!user||!await bcrypt.compare(data.password,user.passwordHash)) return NextResponse.json({error:'Invalid credentials'},{status:401}); await setSession(user._id.toString()); return NextResponse.json({ok:true}); }
