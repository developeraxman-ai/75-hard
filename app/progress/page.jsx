import {AppShell} from '@/components/AppShell'; import {AttemptStatusCard} from '@/components/AttemptStatusCard'; import {ProgressPhotoGrid} from '@/components/ProgressPhotoGrid'; import {FailureHistory} from '@/components/FailureHistory'; import {connectDb} from '@/lib/db'; import {requireUser} from '@/lib/auth'; import {ensureActiveAttempt,serialize} from '@/lib/challenge'; import Attempt from '@/models/Attempt'; import DailyLog from '@/models/DailyLog'; import ImpulseEvent from '@/models/ImpulseEvent';
export default async function Progress(){await connectDb(); const user=await requireUser(); const current=await ensureActiveAttempt(user); const attempts=await Attempt.find({userId:user._id}).sort({createdAt:-1}); const logs=await DailyLog.find({userId:user._id}).sort({date:-1}); const events=await ImpulseEvent.find({userId:user._id}).sort({createdAt:-1}); const completed=logs.filter((l)=>l.completed).length; const failedAttempts=attempts.filter((a)=>a.status==='failed').length; const contract=events.filter((e)=>e.finalChoice==='contract').length; const impulse=events.filter((e)=>e.finalChoice==='impulse').length; return <AppShell title="Progress" kicker="Evidence beats mood."><div className="space-y-4"><AttemptStatusCard attempt={serialize(current)}/><section className="grid grid-cols-2 gap-3"><div className="card"><p className="text-3xl font-black">{completed}</p><p className="text-stone-400">Completed days</p></div><div className="card"><p className="text-3xl font-black">{failedAttempts}</p><p className="text-stone-400">Failed attempts</p></div><div className="card"><p className="text-3xl font-black">{events.length}</p><p className="text-stone-400">Impulse events</p></div><div className="card"><p className="text-3xl font-black">{contract}/{impulse}</p><p className="text-stone-400">Contract / impulse</p></div></section><section className="card"><h2 className="text-xl font-black">Attempt history</h2><p className="text-stone-400">Previous attempts are not deleted. They show the pattern.</p><ul className="mt-4 space-y-2">{attempts.map((a)=><li key={a._id} className="flex justify-between border-b border-command-line py-2"><span>{new Date(a.startDate).toLocaleDateString()}</span><span className="uppercase text-command-gold">{a.status}</span></li>)}</ul></section><section className="card"><h2 className="text-xl font-black">Daily completion list</h2><ul className="mt-3 space-y-1 text-sm">{logs.map((l)=><li key={l._id} className="flex justify-between"><span>Day {l.dayNumber}</span><span>{l.completed?'Complete':l.failed?'Failed':'Open'}</span></li>)}</ul></section><section className="card"><h2 className="mb-3 text-xl font-black">Progress photos</h2><ProgressPhotoGrid logs={serialize(logs)}/></section><FailureHistory logs={serialize(logs)} events={serialize(events)}/></div></AppShell>}
